{
  "name": "firelight-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp index.html tutorial.html _headers favicon.ico favicon.svg dist/",
    "deploy": "npm run build && npx wrangler pages deploy ./dist --project-name=firelight --branch=main"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
